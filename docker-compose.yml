services:
  gameslibrary:
    image: node:24-slim
    hostname: gameslibrary
    container_name: gameslibrary
    ports:
      - "8001:5173"
    volumes:
      - .:/app
    working_dir: /app
    command: npm run dev -- --host 0.0.0.0
    restart: unless-stopped
